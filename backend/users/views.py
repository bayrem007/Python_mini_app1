from rest_framework.views import APIView


class UsersPlaceholderView(APIView):
    """
    Users-related endpoints can live here if you prefer, but this project centralizes HTTP in `api/`.
    """

    pass
